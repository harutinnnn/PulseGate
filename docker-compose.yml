version: '3.8'
services:
  pulsegate:
    build: .
    ports:
      - "8081:8080"
    environment:
      - PORT=8080
      - DATABASE_PATH=/database.db
      - WORKER_CONCURRENCY=10
    volumes:
      - .:/app
    restart: unless-stopped